// We'll see later to make it work on local dev too.
// Not usefull right now
// ----------------------------------------

/* import Middleware from './middleware.js';

Middleware.mediasToNetlify = function ({ req, app }) {
    if (process.server) return;
    console.log('je suis le middleware');
}; */
